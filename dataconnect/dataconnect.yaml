specVersion: "v1"
serviceId: "peerlink"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "peerlink-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
